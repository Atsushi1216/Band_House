<div class="container">
  <div class="row">
    <div class="mainContents col-sm-12 col-sm-push-4 col-md-12 col-md-push-4">
      <h2>Log in</h2>

      <div class="container form-contents">
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>

        <div class="field form-group row">
          <div class="col-md-12 mb-6">
            <%= f.label :"メールアドレス", class:"form-label" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control",required: true %>
          </div>
        </div>

        <div class="field form-group row">
          <div class="col-md-12 mb-6">
            <%= f.label :"パスワード", class:"form-label" %>
            <%= f.password_field :password, autocomplete: "current-password", class:"form-control",required: true %>
          </div>
        </div>
          <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>
        <div class="actions">
          <%= f.submit "ログイン", class:"form-button" %>
        </div>
      <% end %>

        <div class="form-comment text-right">
          <span>会員登録がお済みでない方</span>
          <span><%= render "devise/shared/links" %></span>から新規登録を行なってください。
        </div>
      </div>
    </div>
  </div>
</div>
